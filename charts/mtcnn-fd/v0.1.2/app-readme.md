# mtcnn-fd

mtcnn-fd is an AI demo function developped by QZCloud, which now deployed as the default demo in FDN.

It's a face detection API using mtcnn.

## Introduction

This chart bootstraps a mtcnn-fd AI deployment on a [Kubernetes](http://kubernetes.io) cluster using the [Helm3](https://helm.sh) package manager.
